version: '3.1'
services:
        ubuntu:
                image: ubuntu:latest
                container_name: cont1
                command: mkdir dock
                command: touch dock/change_me.txt
		command: chmod 777 /dock/change_me.txt
                volumes:
                        - ${PWD}/:/dock 